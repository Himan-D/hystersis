[package]
license = "MIT"
authors = ["Hystersis AI"]
name = "hystersis-workspace"
version = "0.1.220-alpha.4"
edition.workspace = true
description = "Core host-local workspace library (FS, VCS, execution, discovery) for hystersis-shell and remote sampler"

[dependencies]
anyhow = { workspace = true }
arc-swap = { workspace = true }
dunce = { workspace = true }
hystersis-version = { workspace = true }
async-stream = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
fs2 = { workspace = true }
dirs = "6"
parking_lot = { workspace = true }
git2 = { version = "0.20", default-features = false, features = ["vendored-libgit2"] }
ignore = { workspace = true }
regex = { workspace = true }
rand = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = [
    "fs",
    "io-std",
    "macros",
    "process",
    "rt",
    "rt-multi-thread",
    "sync",
    "time",
] }
tracing = { workspace = true }
uuid = { workspace = true, features = ["v4", "v5", "v7"] }
hystersis-agent = { path = "../hystersis-agent" }
hystersis-tools = { path = "../hystersis-tools" }
hystersis-tools-api = { path = "../hystersis-tools-api" }
# `DiagHandle`, held by the hub config and threaded through `connect_hub`.
hystersis-diag-server = { workspace = true }
hystersis-workspace-client = { path = "../hystersis-workspace-client" }
# Daemon/process-lifecycle machinery for `bin/workspace_server.rs` only; the
# library never references it.
hystersis-workspace-daemon = { path = "../hystersis-workspace-daemon" }
hystersis-workspace-types = { path = "../hystersis-workspace-types" }
hystersis-config = { workspace = true }
# Leaf config value types (RemoteSettings, BoolFlag) for the folder-trust decision.
hystersis-config-types = { workspace = true }
toml = { workspace = true, features = ["preserve_order"] }
dashmap = { workspace = true }
agent-client-protocol = { workspace = true }
hystersis-acp-lib = { workspace = true }
hystersis-codebase-graph = { path = "../hystersis-codebase-graph" }
hystersis-paths = { path = "../hystersis-paths" }
base64 = { workspace = true }
infer = { workspace = true }
sha2 = { workspace = true, features = ["force-soft"] }
bstr = "1.12.1"
hashbrown = "0.16.1"
nohash-hasher = "0.2.0"
rustc-hash = "2.1.1"
# Backs `file_system`'s fuzzy path picker (matcher + background daemon).
hystersis-fuzzy-file-search = { workspace = true }
indexmap = { workspace = true, features = ["serde"] }
smallvec = { workspace = true }
chrono = { workspace = true }
tree-sitter = "0.25.10"
tree-sitter-bash = "0.25"
url = { workspace = true }
hystersis-env = { workspace = true }
humantime-serde = { workspace = true }
prometheus = { workspace = true }
glob = "0.3"
hystersis-sandbox = { path = "../hystersis-sandbox", default-features = false }
hystersis-hooks = { path = "../hystersis-hooks" }
hystersis-hunk-tracker = { path = "../hystersis-hunk-tracker" }
# `metrics` enables the SDK's metric-donation client (periodic Prometheus
# registry gather → OTLP → hub donation pump); see metric_donation_reporter.
hystersis-computer-hub-sdk = { workspace = true, features = ["metrics"] }
hystersis-computer-hub-mcp-adapter = { path = "../../common/hystersis-computer-hub-mcp-adapter" }
hystersis-mcp = { path = "../hystersis-mcp" }
hystersis-file-utils = { path = "../hystersis-file-utils" }
hystersis-session-events = { workspace = true }
hystersis-auth = { path = "../hystersis-auth" }
hystersis-telemetry = { workspace = true }
hystersis-tty-utils = { workspace = true }
reqwest = { workspace = true }
hystersis-tool-protocol = { workspace = true }
hystersis-tool-runtime = { workspace = true }
hystersis-tool-types = { workspace = true }
tokio-util = { workspace = true, features = ["rt"] }
urlencoding = "2"
hystersis-fast-worktree = { path = "../hystersis-fast-worktree", features = ["metadata"] }
# tonic: Status/Code mapping for deploy errors in workspace_ops.
tonic = { workspace = true }
sha1 = { workspace = true, optional = true }
zip = { workspace = true, optional = true }

hystersis-fsnotify = { path = "../hystersis-fsnotify" }
clap = { workspace = true }
tracing-subscriber = { workspace = true }
# "enable" required for the SDK's spans to record at all.
fastrace = { workspace = true, features = ["enable"] }
hystersis-tracing = { workspace = true }
hystersis-extra-ca = { workspace = true }
tokio-tungstenite = { workspace = true, features = ["rustls-tls-webpki-roots"] }
tempfile = { workspace = true }
zstd = { workspace = true }
wait-timeout = { workspace = true }

# Raw fd/dirent/poll calls in foreign-session capability probes, the fuzzy
# walker's rlimit guard, and the envrc reader.
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(unix)'.dev-dependencies]
nix = { workspace = true, features = ["signal", "process"] }

[[bin]]
name = "hystersis-workspace-server"
path = "src/bin/workspace_server.rs"

[[bin]]
name = "workspace-server-probe"
path = "src/bin/workspace_server_probe.rs"

[features]
default = ["sandbox-enforce"]
compression = []
test-support = []
test-util = []
sandbox-enforce = ["hystersis-sandbox/enforce"]
default-bazel = [
    "sandbox-enforce",
    ]

[dev-dependencies]
tempfile = { workspace = true }
axum = { workspace = true }
tokio = { workspace = true, features = ["test-util"] }
hystersis-test-utils = { path = "../../common/hystersis-test-utils" }
# Decode tar.gz archives produced by workspace upload helpers in tests.
flate2 = { workspace = true }
tar = { workspace = true }

[package.metadata.cargo-shear]
ignored = ["tokio-tungstenite"]

[lints]
workspace = true
