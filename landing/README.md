# code.hystersis.com — Hystersis ASCII site

Pure ASCII, black & white. `curl -fsSL https://code.hystersis.com/install.sh | sh`

- React + Vite
- `npm run dev` — localhost:8765
- `npm run build` — dist/
